<view class="container">
  <view class="card header">
    <view class="title">代理点订单</view>
    <view class="subtitle">接单、配送、自提核销都在这里处理</view>
  </view>

  <view class="card" wx:for="{{orders}}" wx:key="id">
    <view class="order-top">
      <view class="order-no">{{item.orderNo}}</view>
      <view class="status">{{item.statusText}}</view>
    </view>
    <view class="muted">{{item.deliveryText}} · {{item.buildingName}} {{item.roomNo}}</view>
    <view class="amount">¥{{item.payAmount}}</view>
    <view class="actions">
      <button size="mini" bindtap="acceptOrder" data-id="{{item.id}}">接单</button>
      <button size="mini" class="primary-btn" bindtap="completeOrder" data-id="{{item.id}}">完成</button>
    </view>
  </view>
</view>
